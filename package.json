{
  "name": "automatizacion-pujas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "typescript": "^5.5.0",
    "@types/node": "^22.13.8",
    "@types/dotenv": "^6.1.1"
  }
}
